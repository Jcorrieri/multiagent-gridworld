# New Installation steps
- Create conda env with python=3.12
- Install Ray & PyTorch using `pip install "ray[rllib]" torch`
    - Installs ray, torch, networkx, cuda (?), a lot
- Install pygame, pettingzoo, matplotlib
